%{
#include <stdio.h>
%}

%%
(www\.[a-zA-Z0-9]+\.(com|in))   { printf("It is valid\n"); }
.+                              { printf("It is not valid\n"); }
%%

int yywrap() { return 1; }

int main() {
    printf("Enter the URL: ");
    yylex();
    return 0;
}
